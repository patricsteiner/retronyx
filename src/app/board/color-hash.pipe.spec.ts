import { ColorHashPipe } from './color-hash.pipe';

describe('ColorHashPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorHashPipe();
    expect(pipe).toBeTruthy();
  });
});
